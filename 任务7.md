# 任务7：系统补强冲刺——知识库达标、Agent 增强与工程收尾

## 负责人：全员，五个板块并行


| 成员  | 姓名   | 分工                                         |
| ------- | -------- | ---------------------------------------------- |
| 成员1 | 高翊轩 | 7.1 主力：文本情感真实模型（感知增强板块）   |
| 成员2 | 高天阔 | 7.2 主力：知识库达标扩充（123 → 200+）      |
| 成员3 | 单嵩然 | 7.3 主力：多轮记忆与上下文管理（Agent 板块） |
| 成员4 | 胥庆阳 | 7.4 主力：前端记忆交互         |
| 成员5 | 吴鑫涛 | 7.5 主力：工程收尾、材料与验收统筹        |

板块分工与以往一致；每个板块内的具体方案、技术选型与实现方式由负责人自行决定，可充分发挥自己的想法。

---

## 背景

任务 6 完成后，真实面部分析、标准化量表与危机干预均已部署验收（2026-09-04）。但对照申报书承诺与结项要求，仍存在几处硬差距，直接影响结项评审：

1. **知识库仅 123 条**，申报书承诺 ≥200 条，缺口约 77 条。
2. **文本情绪判断纯靠 LLM**，`/v1/text/analyze-sentiment` 端点未实现，缺模型依据。
3. **多轮对话每轮独立分析**，Agent 不接收历史，缺乏上下文管理。
4. 任务 6 的三个部署期修复（`bb8f520` / `09dfbd2` / `a473c64`）尚未全员同步。

任务 7 目标：**两周内补齐以上缺口**，为后续结项评估与材料准备打好基础。

---

## 高翊轩 — 文本情感真实模型（7.1 主力）

**我要做什么：**

- 实现 `/v1/text/analyze-sentiment` 端点（或等价能力），用真实情感分类模型替换纯 LLM 判断
- 调研选定中文情感模型（如 Erlangshen/RobertaA-Emotion 七类情绪模型等），输出与前端 14 类情绪标签的兼容映射
- Agent 状态机"理解"节点改为调用该端点，LLM 仅做综合与表达

**⚠️ 服务器约束（4核8G，无升级空间）：**
- **离线部署**：服务器连不上国际网络（huggingface 不可达），模型必须本地下载后打包 scp 进服务器，参照 OpenFace 离线构建方案；模型文件需持久化卷缓存
- **模型放置**：并入 knowledge 或 agent 容器（torch 运行时已加载，边际成本仅模型权重 300–500 MB）；**不要并入 vision 容器**（OpenFace 峰值需留空间）；不新开独立容器
- **模型选型**：选 100–300M 参数的 BERT 类中文情感模型，CPU 推理短文本约几百毫秒
- **不动现有容器限额**：compose 中各容器 memory 限制已验证，任务7 不得改动
- **磁盘**：构建新镜像前先 `docker image prune -a` 清理旧镜像腾空间
- **swap 兜底（可选）**：服务器默认无 swap，可加 2G swapfile 防偶发峰值 OOM

**自由发挥（不限于所列）：**

- 模型选择由自己调研定，可同时对比 2 个模型选优，留存对比数据
- 可输出情绪强度（0-1）而非仅标签，前端情绪曲线更平滑
- 设计"模型分数 + LLM 综合"的融合策略，避免模型误判影响共情体验

**我的交付物：** 情感分析端点实现、模型选择对比记录、单元测试

**我的验收标准：**

- `/v1/text/analyze-sentiment` 返回真实模型推理结果（非硬编码、非 LLM 代劳）
- 输出与前端 14 类情绪映射兼容，前端展示正常
- 与 Agent 全链路联调通过
- 纯文字输入路径完全不受影响（回归测试通过）
- 上线后 `free -h` / `docker stats` 常态内存 < 7 GB，服务运行平稳
- 新增测试全部通过

---

## 高天阔 — 知识库达标扩充（7.2 主力）

**我要做什么：**

- **量表解读深挖**：现有 15 条 → 40 条以上（PHQ-9 逐维度解读、GAD-7 分级解读、双量表组合解读、作答后行动建议）
- **议题科普扩充**：学业压力、人际交往、睡眠困扰、拖延、家庭关系、恋爱困扰、自卑、情绪管理、焦虑应对、正念练习等 10 个主题，每主题约 5 条，共约 50 条
- **转介资源条目**：南京大学心理中心预约方式、南京脑科医院等专业机构、12355/400-161-9995 热线使用说明、就医流程、校内资源等 ≥10 条
- 所有条目标注真实来源 `【来源：xxx】`，来源必须可查证（WHO、NIMH、国内指南、教材等），不得编造

**自由发挥（不限于所列）：**

- 为条目增加关键词 / 适用场景标签，提升检索命中率
- 写一个检索质量自测小脚本（随机抽 20 个问题，人工/LLM 打分相关度）
- 设计条目分级（通用科普 / 专业解读），检索时按用户意图调整权重

**我的交付物：** 知识库新增条目（≥77 条，总量 ≥200）、可选的自测脚本

**我的验收标准：**

- 知识库总量 ≥200 条，全部带来源标注
- 抽查 10 条来源真实可查
- 量表解读、科普、转介三类检索均可命中
- 启动自动 upsert 后无需清卷即可生效

---

## 单嵩然 — 多轮记忆与上下文管理（7.3 主力）

**我要做什么：**

- **历史接入**：对话历史传入 Agent（`/chat` 及兼容路由），Agent 不再每轮独立分析
- **情绪摘要记忆**：每轮对话后由 LLM 生成一段会话情绪状态摘要（可含量表分数、面部情绪历史），下轮注入提示词——这是"上下文工程"的核心
- **截断策略**：历史不无限膨胀，设计长度/轮次上限与压缩规则
- 持久化可选：Redis 或文件级会话存储，服务重启不丢（时间紧可降级为内存态 + 说明）
- 与胥庆阳约定历史透传的字段格式后并行开发

**自由发挥（不限于所列）：**

- 摘要格式自定（结构化 JSON 或自然语言），可包含"关注点变化轨迹"
- 可设计类遗忘曲线的压缩（旧轮次摘要越来越粗粒度）
- 思考如何在长会话中保持对核心目标的专注、避免上下文遗忘

**我的交付物：** Agent 多轮记忆实现、单元测试

**我的验收标准：**

- 多轮对话中 Agent 能引用之前轮次的内容（实测可复现）
- 情绪摘要机制跑通，摘要随会话更新
- 长对话（10 轮以上）不报错、历史有截断
- 与 7.1 的情感注入互不冲突，全链路联调通过

---

## 胥庆阳 — 前端记忆交互（7.4 主力）

**我要做什么：**

- **历史透传前端**：对话历史随请求传入 Agent，与单嵩然约定字段格式后并行开发
- **情绪轨迹卡片**：前端展示会话情绪摘要/关注点轨迹，与已有情绪曲线联动
- **UI 终验配合**：组长终验发现的前端问题修复

**自由发挥（不限于所列）：**

- 情绪轨迹卡片展示形式自定（时间线 / 摘要气泡等）
- 前端小体验优化（加载态、错误提示、移动端适配）

**我的交付物：** 前端记忆交互组件、前端测试

**我的验收标准：**

- 浏览器可见会话历史与情绪轨迹，多轮对话体验连贯
- 新增前端测试全部通过

---

## 吴鑫涛 — 工程收尾、材料与验收统筹（7.5 主力）

**我要做什么：**

- **代码同步**：组织全员 pull 最新 main（含 3 个部署期修复），消除本地分叉
- **nginx 统一入口**：服务器安装 nginx，浏览器直连 80 端口（配置已就绪，仅剩部署）
- **UI 浏览器终验**：量表 + 聊天 + 照片完整流程实测，问题登记 bug 单并分配到人
- **文档更新**：项目完成情况.md 补任务 6；README 补最新架构图与 API 表
- **用户使用说明初稿**：结项必交材料，覆盖文字对话、照片上传、量表、危机提示等主要流程

**自由发挥（不限于所列）：**

- 整理"部署踩坑集"（国内镜像、离线构建），为结项报告工程章节备料
- 服务器监控与日志巡检习惯固化成脚本

**我的交付物：** nginx 部署、UI 验收记录、更新后的文档、用户使用说明初稿

**我的验收标准：**

- 浏览器 80 端口直连全流程可用
- 全员代码与 GitHub main 一致
- UI 终验通过，发现问题全部登记并分配到人
- 四容器 docker compose up 全 healthy
- 文档与当前系统一致（无过期内容）
- 使用说明覆盖系统主要使用流程

---

## 依赖关系与完成顺序

```
7.1 (情感模型) ————————┐
7.2 (知识库) ———————————┤ 大部分互相独立，Agent 侧注入联调时协调
7.3 (多轮记忆) ————————┤
7.4 (前端记忆交互) ———┘ 与 7.3 有字段约定依赖
7.5 (工程收尾) ————————— 代码同步是所有开发的前置，nginx 收尾
```


| 阶段 | 谁              | 做什么                            | 前置条件                   |
| :----: | ----------------- | ----------------------------------- | ---------------------------- |
|  1  | 吴鑫涛          | 全员代码同步 + nginx 部署         | 无                         |
|  1  | 高翊轩          | 情感模型本地下载 + 打包上传服务器 | 无（尽早启动，走离线流程） |
|  1  | 高天阔          | 量表解读条目扩充                  | 无                         |
|  1  | 胥庆阳 + 单嵩然 | 约定历史透传字段格式              | 无                         |
|  2  | 高翊轩          | 情感端点实现                      | 模型上传完成               |
|  2  | 单嵩然          | 多轮记忆开发                      | 代码同步完成               |
|  2  | 胥庆阳          | 前端记忆交互开发                  | 字段约定完成               |
|  3  | 单嵩然 + 高翊轩 | Agent 侧情感注入与记忆联调        | 各自实现完成               |
|  4  | 吴鑫涛          | UI 终验 + 文档更新                | 前端功能就绪               |
|  5  | 全员            | 集中联调 + 组长验收               | 以上全部                   |

## 时间安排（2 周）

- **第 1 周（9/7–9/13）**：pull 代码；高翊轩离线模型准备；高天阔完成量表解读部分；单嵩然/胥庆阳字段约定 + 记忆开发；吴鑫涛 nginx 部署 + 启动 UI 终验
- **第 2 周（9/14–9/20）**：高天阔收尾达标；高翊轩/单嵩然/胥庆阳完成开发并开始联调；吴鑫涛文档更新
- **9/21–9/22**：集中联调 + 组长验收

---

## 验证清单（组长用）

- [ ]  知识库总量 ≥200 条，全部带来源标注，抽查来源真实
- [ ]  `/v1/text/analyze-sentiment` 返回真实模型结果
- [ ]  多轮对话 Agent 能引用历史；10 轮长对话不报错
- [ ]  nginx 80 端口直连可用
- [ ]  全员代码与 GitHub main 一致，新增提交全量推送
- [ ]  UI 浏览器终验通过（量表 + 聊天 + 照片）
- [ ]  新增测试全部通过，GitHub Actions 全绿
- [ ]  四容器 docker compose up 全 healthy
- [ ]  文档更新完毕；用户使用说明初稿完成
