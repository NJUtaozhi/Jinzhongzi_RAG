# 任务6：面部分析真实落地 + 心理量表与危机干预

## 负责人：全员（两个子任务并行）

| 成员  | 姓名   | 分工                                        |
| ------- | -------- | --------------------------------------------- |
| 成员1 | 高翊轩 | 6.1 主力：OpenFace 部署与 Vision 服务适配   |
| 成员5 | 吴鑫涛 | 6.1 支持：服务器升配、Docker 编排、经费报销 |
| 成员4 | 胥庆阳 | 6.2 主力：前端量表界面与计分                |
| 成员3 | 单嵩然 | 6.2 主力：Agent 量表集成与危机评估规则      |
| 成员2 | 高天阔 | 6.2 支持：量表解读知识入库                  |

---

## 背景

任务5 完成后系统全链路可用，但存在两个关键短板：

1. **面部数据为占位值**：OpenFace 未部署到服务器（2.2.0 版本发布于 2018 年，官方仅支持 Ubuntu 18.04/20.04；现服务器为 24.04 且内存 3.6GB 无余量），前端"面部情绪""AU12 微笑强度"均显示兜底值。
2. **缺乏标准化评估依据**：系统只有自由对话，无专业心理量表支撑；Agent 无危机识别与转介机制，存在安全短板。

任务6 目标：**补上真实面部分析能力**（经费支持），**增加标准化心理评估与危机干预**（核心专业性卖点），为 11 月结项答辩做准备。

---

## 高翊轩 — OpenFace 部署与 Vision 服务适配（6.1 主力）

**我要做什么：**

- **OpenFace 部署**：优先方案 A——Docker 内用 `ubuntu:20.04` 编译 OpenFace 2.2.0（OpenCV + dlib + OpenBLAS），下载 AU 检测模型，命令行验证 `FaceLandmarkImg`；方案 A 失败则启用方案 B（MediaPipe / FER 轻量替代）
- **Vision 服务适配**：挂载 OpenFace 二进制目录，设置 `OPENFACE_BIN` 环境变量，验证 AU 解析
- **Agent 兼容路由修改**：`/v1/agent/analyze` 改为读取真实 AU 与 RAG 知识来源（替换当前硬编码的 `au12_r_smile_intensity` 与 `advice_source`）
- **全链路联调**：前端传图 → Agent → Vision 返回真实 AU → 前端展示真实数据

**我的交付物：** openface Docker 镜像、更新后的 `vision_service/` 与 Agent 兼容路由

**我的验收标准：**

- `curl http://localhost:8001/health` 返回 `openface_available: true`
- 上传真实人脸图片返回真实 AU 数据（非 503）
- Agent 兼容路由返回真实 AU12 与建议来源（非硬编码占位值）
- 前端上传照片显示真实面部情绪与 AU12 数值
- 单张图片分析耗时 < 10 秒
- 无人脸图片返回 `40010` 错误码，服务不崩溃

---

## 吴鑫涛 — 服务器升配、Docker 编排、经费报销（6.1 支持）

**我要做什么：**

- **服务器升配**：腾讯云控制台将轻量服务器升配至 4核8GB（或新购），备份 `~/jinzhongzi`、`.env`，导出 docker 镜像
- **Docker 编排**：openface 容器接入 `docker-compose.yml`，调整各容器资源限制
- **Dockerfile 统一**：清理"FROM 旧镜像"临时方案，提交正式版 Dockerfile 至 GitHub
- **经费报销**：留存云服务费发票，按流程整理提交

**我的交付物：** 4核8GB 服务器（已部署）、更新后的 `docker-compose.yml` 与正式版 Dockerfile

**我的验收标准：**

- 服务器升配完成，发票留存
- 含 openface 在内的全量容器 `docker compose up -d --build` 正常启动
- GitHub 全量提交，Dockerfile 统一正式版

---

## 胥庆阳 — 前端量表界面与计分（6.2 主力）

**我要做什么：**

- **量表页面**：PHQ-9 / GAD-7 入口（侧边栏，进入对话前可选做），单选按钮 + 进度条
- **计分展示**：完成后显示总分与严重程度分级
- **字段约定**：与单嵩然约定 `assessment` 字段格式后并行开发
- **测试**：前端量表交互测试

**我的交付物：** `frontend/` 量表组件、前端测试

**我的验收标准：**

- 量表完成后显示总分 + 分级（如 PHQ-9 12 分 → "中度抑郁倾向，建议关注"）
- 新增测试全部通过

---

## 单嵩然 — Agent 量表集成与危机评估规则（6.2 主力）

**我要做什么：**

- **量表集成**：`ChatRequest` 增加 `assessment` 字段（量表名/总分/分级），状态机"感知"节点写入 `runtime_context`，LLM prompt 加入评分依据
- **字段约定**：与胥庆阳约定 `assessment` 字段格式
- **危机评估与转介**：安全校验节点增加规则——PHQ-9 ≥15 或第 9 题（自伤意念）≥1 → 标记高风险 → 回复附全国心理援助热线（12355、400-161-9995）与就近就医建议，使用危机干预话术
- **测试**：量表计分单元测试、危机阈值规则测试

**我的交付物：** Agent 危机评估逻辑、新增测试

**我的验收标准：**

- 高风险输入（自伤意念）→ 回复包含热线电话与就医建议
- 量表分数进入最终分析报告，Agent 回复引用评分依据
- 新增测试全部通过

---

## 高天阔 — 量表解读知识入库（6.2 支持）

**我要做什么：**

- **知识库扩充**：将 PHQ-9 / GAD-7 评分解释、分级建议录入知识库（≥10 条），标注 `【来源：xxx】`

**我的交付物：** 知识库新增条目

**我的验收标准：**

- 知识库新增 ≥10 条量表解读条目
- 量表解读条目可被正常检索

---

## 依赖关系与完成顺序

```
6.1 (OpenFace 部署) ———————————————— 独立推进
6.2 前端量表 UI ──→ 6.2 Agent 字段扩展 ──→ 6.2 危机规则 ──→ 联调
6.2 知识库扩充 ———————————————— 与上述并行
```

两组并行推进，完成先后顺序如下（同一阶段内可同时进行）：

| 阶段 | 谁 | 做什么 | 前置条件 |
|:---:|---|---|---|
| 1 | 吴鑫涛 | 经费申请 + 服务器升配至 4核8GB | 无（依赖经费审批，尽早启动） |
| 1 | 胥庆阳 + 单嵩然 | 约定 `assessment` 字段格式 | 无 |
| 1 | 高天阔 | 量表解读知识入库（≥10 条） | 无（可立即开始） |
| 2 | 高翊轩 | OpenFace 部署 + Vision 服务适配 | 服务器升配完成 |
| 2 | 胥庆阳 | 前端量表 UI + 计分展示 | 字段格式约定完成 |
| 2 | 单嵩然 | Agent 量表集成（`assessment` 字段 + 状态机） | 字段格式约定完成 |
| 3 | 单嵩然 | 危机评估规则 + 转介话术 | Agent 量表集成完成 |
| 3 | 高翊轩 | Agent 兼容路由改真实 AU + 全链路联调 | OpenFace 部署完成 |
| 4 | 吴鑫涛 | Docker 编排调整 + Dockerfile 统一 + 经费报销 | openface 镜像就绪 |
| 5 | 全员 | 集中联调 + 组长验收 + 结项材料 | 以上全部 |

## 时间安排

- 8 月下旬：经费申请 + 服务器升配 + 量表字段格式约定
- 9 月上旬：6.1 OpenFace 部署完成；6.2 前端量表 + Agent 集成完成
- 9 月中下旬：危机规则 + 知识库扩充 + 全链路联调
- 10 月：系统打磨 + 结项材料（海报、报告）

---

## 验证清单 (组长用)

- [ ]  服务器升配至 4核8GB，发票留存
- [ ]  `vision /health` 返回 `openface_available: true`
- [ ]  真实人脸照片 → 真实 AU 数据（非占位值）
- [ ]  无人脸图片 → `40010`，服务不崩
- [ ]  前端量表计分正确（PHQ-9 / GAD-7）
- [ ]  高风险输入触发热线转介
- [ ]  量表分数出现在最终分析报告
- [ ]  知识库新增 ≥10 条量表解读条目
- [ ]  新增测试全部通过
- [ ]  GitHub 全量提交，Dockerfile 统一正式版
