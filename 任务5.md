# 任务5：Bug修复、联调测试与最终交付

## 负责人：全员

| 成员 | 姓名 | 分工 |
|------|------|------|
| 成员1 |  | Vision 服务修复与完善 |
| 成员2 |  | Knowledge 服务修复与完善 |
| 成员3 |  | Agent 服务修复与完善 |
| 成员4 |  | Frontend 修复与完善 |
| 成员5 |  | 基础设施修复、CI/CD、最终集成 |

---

## 背景

任务1-4 已完成后端四个微服务的开发与云服务器部署，全链路 `Frontend → Agent → Knowledge` 已跑通。但存在 8 个关键 bug 和若干工程化缺口（零测试覆盖、无语义化日志、无 CI/CD）。任务5目标：修复全部已知问题 → 完成联调测试 → 准备最终答辩材料。

---

## 任务5.1 Vision 面部分析服务修复

**当前问题：**

1. `vision_service/main.py` 第 11 行：OpenFace 二进制路径硬编码为 `D:\学习资料\科研类项目\...\FaceLandmarkImg.exe`，忽略 `OPENFACE_BIN` 环境变量
2. 第 173 行：裸 `except Exception` 无错误分类
3. 全程使用 `print()` 无结构化日志
4. Agent 调用的 `/v1/text/analyze-sentiment` 端点未实现
5. 零测试覆盖

**任务内容：**

| 步骤 | 说明 |
|------|------|
| 1 | 修改 `OPENFACE_BIN` 从 `os.environ.get("OPENFACE_BIN", "FaceLandmarkImg.exe")` 读取 |
| 2 | 启动时用 `shutil.which()` 检测 OpenFace 是否可用；不可用则在请求时返回 503 + 友好提示 |
| 3 | 错误处理改为分类 `FileNotFoundError` / `TimeoutExpired` / `ValueError`，分别返回 `40010` / `50001` / `40011` |
| 4 | 用 Python `logging` 模块替换 `print()`，每条请求记录耗时 |
| 5 | 编写至少 5 个单元测试（放 `vision_service/tests/`） |

**交付物：** `vision_service/main.py`（修改），`vision_service/tests/`（新增）

**验收标准：**
- Docker 内启动时不崩溃，无 OpenFace 时调用接口返回 `{"code": 50003, "msg": "OpenFace 分析引擎未部署..."}`
- `OPENFACE_BIN` 环境变量可正确覆盖默认路径
- 5 个测试通过

---

## 任务5.2  Knowledge 知识库检索服务修复

**当前问题：**

1. `/v1/knowledge/retrieve` 端点无 try/except，`model.encode()` 或 `collection.query()` 失败直接 500
2. RAGClient（Agent 端）解析响应的路径为 `raw.get("results", raw.get("data", []))`，但 Knowledge 返回 `{data: {results: [...]}}`，两层嵌套导致解析失败
3. 全程 `print()` 无结构化日志
4. 知识库 46 条偏少，条目无来源标注
5. 零测试覆盖

**任务内容：**

| 步骤 | 说明 |
|------|------|
| 1 | `/v1/knowledge/retrieve` 加 try/except，返回标准错误格式 `{code, msg, data: null}` |
| 2 | 与成员3确认响应格式：`data.results` 路径一致，同步修复 RAGClient 解析 |
| 3 | 用 Python `logging` 模块记录每次检索的 query、命中数、耗时 |
| 4 | 扩充 `knowledge_data.txt` 至 80 条以上，覆盖焦虑、抑郁、睡眠、CBT、正念、学业压力、人际关系、自尊等 8 个主题 |
| 5 | 编写至少 5 个单元测试（放 `knowledge_service/tests/`） |

**交付物：** `knowledge_service/main.py`（修改），`knowledge_service/knowledge_data.txt`（扩充），`knowledge_service/tests/`（新增）

**验收标准：**
- `curl -X POST -d '{"query":"焦虑","top_k":3}' localhost:8002/v1/knowledge/retrieve` 返回 3 条结果
- 空知识库或模型加载失败时返回错误 JSON 而非 500
- 知识库 ≥ 80 条，ChromaDB 数据在 `docker compose down && up` 后保持
- 5 个测试通过

---

## 任务5.3 单嵩然 — Agent 决策服务修复

**当前问题：**

1. `/chat/stream` 返回 HTTP 501（流式输出未实现）
2. `requirements.txt` 缺少 `jsonpatch`（LangGraph 运行时依赖）
3. `/health` 返回 `{"services": {...}}`，但前端期望 `{"dependencies": {...}}`，导致前端健康检查显示异常
4. `services/rag_client.py` 第 149 行响应解析路径与实际不匹配（见 5.2）
5. `services/multimodal_client.py` 中 `HTTPConnection(self._base_url)` 传入完整 URL（如 `http://vision:8001`），应先用 `urlparse` 拆出 host 和 port
6. `api_spec.yaml` 标记 `image` 为必填，但 `/v1/agent/analyze` 实际为可选
7. 全程 `print()` 无结构化日志

**任务内容：**

| 步骤 | 说明 |
|------|------|
| 1 | 实现 `/chat/stream`：用 `StreamingResponse` + `text/event-stream`，将 reply 分块推送 |
| 2 | `requirements.txt` 添加 `jsonpatch>=1.32` |
| 3 | `/health` 同时返回 `services` 和 `dependencies` 两个键 |
| 4 | 修复 `RAGClient._parse_response`：拆出 `data.results` 路径 |
| 5 | 修复 `MultimodalClient._http_post`：用 `urlparse` 解析 `self._base_url` |
| 6 | `api_spec.yaml` 将 `image` 改为 `required: false`，补充 `/chat` 和 `/chat/stream` 定义 |
| 7 | 用 `logging` 模块替换 `print()`，每个 ReAct 节点记录耗时 |

**交付物：** `main.py`（修改），`services/rag_client.py`（修改），`services/multimodal_client.py`（修改），`requirements.txt`（修改），`api_spec.yaml`（修改）

**验收标准：**
- `curl -N -d '{"query":"你好"}' localhost:8003/chat/stream` 收到 SSE 事件流
- `pip install -r requirements.txt` 无缺失依赖错误
- 前端健康检查正确显示四个服务状态
- `api_spec.yaml` 与实际实现一致

---

## 任务5.4  Frontend 前端修复

**当前问题：**

1. `frontend/app.py` 第 19 行：`AGENT_API_URL` 硬编码为 `"http://101.34.68.33:8003/v1/agent/analyze"`，忽略环境变量
2. 第 85 行：健康检查读取 `health.json().get("dependencies", {})`，但 Agent 返回 `services` 键
3. `SENTIMENT_SCORE_MAP` 只映射 4 个情绪标签（positive/neutral/mixed/negative），Agent 还可能返回 anxiety、depression、anger、sadness、fear 等
4. 文本+图模式统一走 `/v1/agent/analyze`（multipart），纯文本模式应走 `/chat`（JSON，更快）
5. 零测试覆盖

**任务内容：**

| 步骤 | 说明 |
|------|------|
| 1 | 改为 `AGENT_API_URL = os.environ.get("AGENT_API_URL", "http://localhost:8003/v1/agent/analyze")` |
| 2 | 健康检查改为 `deps = health.json().get("services", health.json().get("dependencies", {}))` |
| 3 | `SENTIMENT_SCORE_MAP` 扩展至 8+ 标签 |
| 4 | 纯文本模式调用 `/chat` JSON 端点，有图片时才走 `/v1/agent/analyze` multipart |
| 5 | 编写至少 3 个测试：API mock、情绪映射、健康检查解析 |

**交付物：** `frontend/app.py`（修改），`frontend/tests/`（新增）

**验收标准：**
- 环境变量 `AGENT_API_URL` 可覆盖默认值
- 前端页面正常显示四个服务状态
- 纯文本对话走 `/chat`，带图走 `/v1/agent/analyze`
- 3 个测试通过

---

## 任务5.5  系统集成与最终交付

**当前问题：**

1. Docker 健康检查全部使用 `curl`，`python:3.10-slim` 镜像不含 curl → `depends_on: service_healthy` 永远不满足
2. 四服务只能手动逐个 `docker run`，无法 `docker compose up` 一键启动
3. 无 `.dockerignore` → `docker build` 会打包 `.git/`、`__pycache__/` 等
4. 无资源限制，3.6GB 内存可能 OOM
5. 无 CI/CD，部署靠 scp + 手动 docker run
6. 无 Nginx 反向代理，前端直接暴露端口
7. 成员修改代码后无自动化测试验证

**任务内容：**

| 步骤 | 说明 |
|------|------|
| 1 | 四个 `docker/` 下 Dockerfile 各加 `RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*` |
| 2 | `docker-compose.yml` 每个服务加 `mem_limit`（vision: 512M, knowledge: 1.5G, agent: 512M, frontend: 512M） |
| 3 | 创建 `.dockerignore`：排除 `.git/`、`__pycache__/`、`*.pyc`、`.env`、`temp_*/`、`kb_chroma_db/` |
| 4 | 创建 `nginx.conf`：统一 `:80` 入口，`/` → frontend:8501，`/api/agent/` → agent:8003，`/api/knowledge/` → knowledge:8002，`/api/vision/` → vision:8001 |
| 5 | 创建 `.github/workflows/test.yml`：push → `python tests/test_pure.py && python tests/test_state_machine.py` |
| 6 | 更新 `README.md`：补充最终架构图、API 列表、部署步骤、成员分工 |
| 7 | 最终联调：`git clone → docker compose up -d → 全链路测试通过 → 浏览器 http://服务器IP 可用` |

**交付物：** `docker-compose.yml`（修改），`docker/vision/Dockerfile`、`docker/knowledge/Dockerfile`、`docker/agent/Dockerfile`、`docker/frontend/Dockerfile`（修改），`.dockerignore`（新增），`nginx.conf`（新增），`.github/workflows/test.yml`（新增），`README.md`（更新）

**验收标准：**
- `docker compose up -d` 一键启动，四容器全部 healthy
- 浏览器打开 `http://101.34.68.33` 显示前端页面
- 输入"我最近很焦虑" → 收到 Agent 回复 → 情绪曲线更新
- GitHub push → Actions 自动跑测试

---

## 时间线

```
Day 1 — 3  任务 5.1 ~ 5.4  全员并行修复各自服务 bug
Day 3 — 6  任务 5.1 ~ 5.4  继续完善（测试、日志、文档）
Day 4 — 7  任务 5.5        吴鑫涛 基础设施（健康检查、CI/CD、Nginx）
Day 7 — 9  联调             全员参与集成测试，修复最后一轮 bug
Day 9 — 10 交付             Notion 总结页 + PPT + 答辩准备
```

## 依赖关系

```
5.3 (Agent health 加 dependencies)
    ↓
5.4 (Frontend 适配 health 检查)

5.1 + 5.2 + 5.3 + 5.4 (各自完成)
    ↓
5.5.7 (最终联调)
```

---

## 最终验收清单

- [ ] `docker compose up -d` 一次成功，四容器全 healthy
- [ ] Vision `/v1/vision/analyze-face` 正常或返回明确降级提示
- [ ] Knowledge `/v1/knowledge/retrieve` 返回正确检索结果
- [ ] Agent `/chat` 返回完整回复 + 情绪 + 意图
- [ ] Agent `/chat/stream` 返回 SSE 事件流
- [ ] Agent `/v1/agent/analyze` multipart 兼容前端
- [ ] Frontend `http://101.34.68.33:8501` 可访问
- [ ] Nginx `http://101.34.68.33` 统一入口可用
- [ ] 纯文本对话：输入 → 分析 → 回复 → 情绪曲线 全流程正常
- [ ] GitHub Actions `test.yml` push 触发自动测试
- [ ] `README.md` 包含完整部署说明
